@startuml
!pragma useNewPackage
hide empty members

package ska.cdm.messages.central_node {
	package assign_resources.py <<Rectangle>> {
		class DishAllocation {
			receptor_ids : List[str]
		}

		class AssignResourcesRequest {
			subarray_id : int
		}
		AssignResourcesRequest *-- DishAllocation

		class AssignResourcesResponse
		AssignResourcesResponse *-- DishAllocation
	}
	note top of assign_resources.py
		This module contains the ""Request""
		and ""Response"" classes for
		""CentralNode.AssignResources()"".
	end note


	package release_resources.py <<Rectangle>> {
		class ReleaseResourcesRequest {
			subarray_id : int
			release_all : bool
		}
		ReleaseResourcesRequest *-- DishAllocation
	}
	note top of release_resources.py
		This module contains the ""Request"" class
		for ""CentralNode.ReleaseResources()"".
		The response returned by this command is
		not  structured JSON, hence we don't need
		a ""Response"" class to be defined.
	end note

}
note top of ska.cdm.messages.central_node
	The ""ska.cdm.messages.central_node"" package
	contains object models for the JSON payloads
	passed as arguments to and responses returned
	by the TMC CentralNode Tango device.
end note


@enduml