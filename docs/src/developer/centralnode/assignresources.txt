@startuml
hide empty members

package ska.cdm.messages.central_node {
	package assign_resources.py <<Rectangle>> {
		class DishAllocation {
			receptor_ids : List[str]
		}

        class SubBand {
            frq_min : float
            frq_max : float
            nchan : int
            input_link_map : List[List[int]]
        }

        class ScanType {
            st_id : str
            ra : str
            dec : str
            coordinate_system : str
        }
        ScanType *-- "*" SubBand

        class SDPParameters {
        num_stations: integer
        num_channels: integer
        num_polarisations: integer
        freq_start_hz: float
        freq_end_hz: float
        target_fields: Dict[str, Target]
        }

        class Dependency {
            pb_id : str
            pb_type : str
        }

        class SDPWorkflow {
            workflow_id : str
            workflow_type : str
            version : str
        }

        class SDPProcessingBlock {
            pb_id : str
        }
        SDPProcessingBlock *-- "1" SDPWorkflow
        SDPProcessingBlock *-- "1" SDPParameters
        SDPProcessingBlock *-- "*" Dependency

        class SDPConfiguration {
            sdp_id : str
            max_length : float
		}
        SDPConfiguration *-- "*" ScanType
        SDPConfiguration *-- "*" SDPProcessingBlock

		class AssignResourcesRequest {
			subarray_id : int
		}
		AssignResourcesRequest *-- DishAllocation
        AssignResourcesRequest *-- SDPConfiguration

		class AssignResourcesResponse
		AssignResourcesResponse *-- DishAllocation
	}
}

@enduml
